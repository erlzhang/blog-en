language: ruby
rvm:
  - 2.5
script: bundle exec jekyll build

branches:
  only:
  - travis

deploy:
  provider: script
  skip_cleanup: true
  script: rvm use $TRAVIS_RUBY_VERSION do scripts/deploy.rb
  keep_history: true
  on:
    branch: travis